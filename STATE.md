# STATE.md — Active work

What is being worked on right now, and how far along it is — nothing else. **Size budget: ~30 lines.** Everything static lives elsewhere: conventions and the doc index in [CLAUDE.md](./CLAUDE.md), the as-built system in [docs/ARCHITECTURE.md](./docs/ARCHITECTURE.md), a change's narrative (written once) in [CHANGELOG.md](./CHANGELOG.md), the backlog in GitHub issues. The verifier checks this file's claims against reality on its docs-consistency rung.

## Active work

**Plan 12 — vaults + egress-time credential injection** ([docs/plan/12_vaults-credentials.md](./docs/plan/12_vaults-credentials.md), `in-progress`, issue #50). Slices 1–3 merged; slice 4 (the egress gate) lands as several sub-PRs. BYOC delivery and TLS-terminating substitution deliberately split out (#165, #166).

## Tasks

- [x] Slice 1 — cipher seam + infrastructure (PR #168, merged): `internal/secrets` (Cipher iface, `local` + `openbao` backends, contract suite, `secretstest` harness), controlplane/executor plumbing, compose `openbao` + init one-shot, helm StatefulSet + `externalOpenBao` + `localCipher` — all runtime-verified.
- [x] Slice 2 — `/v1/vaults` + credentials CRUD, wire-complete (PR #169, merged): migration 0011, `vcrd` prefix, full auth-union validation, sealed secrets via the cipher, `mcp_oauth_validate` live probe, DIVERGENCES entries + `work.secret` re-pointed at #165.
- [x] Slice 3 — session `vault_ids` attachment (PR #170, merged): `POST /v1/sessions` accepts/validates (existing + unarchived, `FOR SHARE`) and round-trips `vault_ids`; update stays wire-faithfully rejected; DIVERGENCES:28 create-rejection lifted + create-time error-shape inference recorded. Read-time resolution moves to slice 4 (built beside its egress consumers so its shape isn't guessed).
- [ ] Slice 4 — egress gate phase 1, landing as sub-PRs. Gate topology decided (2026-07-24): a shared `cmd/gate` container image, a sidecar on both backends. **Done:** (a) `sandbox.Spec.Env` seam threaded into both backends with `ValidateEnv` key gating (PR #172); (b) `internal/egress` substitution engine — `HostSet` matcher, deterministic `vltph_` placeholder, `Engine.Substitute`, pure + unit-tested (PR #173); (c) read-time resolution (`internal/vaultresolve`, first-vault-wins) + placeholder injection into `Spec.Env`, unsafe-named creds (invalid or reserved, e.g. `PATH`) skipped, `ValidEnvName`/`ReservedEnvName` (PR #174); (d) `internal/gate` — the forward-proxy core: CONNECT host-filter + plain-HTTP host-filter + `egress` substitution, `OnUnreachable` seam, RFC-7230 hop-by-hop hygiene (request + response), bounded body (`MaxBodyBytes`→413), CONNECT half-close/buffered-byte handling, httptest-driven (PR #175); (e) gate-side resolution — `vaultresolve.Credentials` decrypt path over the cipher + shared `winnersFor` with `Bindings` (first-vault-wins parity), fail-closed, lands inert (PR #176); (f) per-session gate tokens — `internal/gatetoken` (`Mint`/`Ensure`/`Authenticate`, fixed session-lifetime, revoke-on-re-mint, hash-only) + migration `0012_session_gate_tokens`, lands inert (PR #177); (g) internal gate-config endpoint + client — `GET /internal/v1/gate/config` (own `gtk_` auth lane) + `internal/gateconfig` client (`Fetch`, `ErrUnauthorized` fail-closed) + DIVERGENCES entry, lands inert (PR #178); (h) `cmd/gate` + `internal/gaterun` runtime — owner-match iptables (v4+v6, verify-listing-before-drop-privileges), fetch-and-swap loop (401→deny-all+terminate, transient→last-known-good), DTO→`gate.Config` convert, `egress` per-credential `Unrestricted`, `egress_request` OTel span, gate image (`docker build --target gate`) + HEALTHCHECK-gated admission (PR #179). (i) Docker gate-pair go-live (PR #183, merged) — gate-first create on the deploy net + HEALTHCHECK-gated admission, sandbox joins `container:<gateID>` with provider-injected proxy env, mint-on-create tokens, gate-aware recovery, opt-in via `CONTROLPLANE_URL`+`EXECUTOR_GATE_IMAGE` (unconfigured = pre-gate fail-closed, non-breaking). (4-ii, PR #186, merged) the real-egress permit path end-to-end — contract rows rewritten to "limited = only allowed_hosts through the gate" via a `Harness` gate seam (K8s gate-less and non-faulting until 4d), the CONNECT-tunnel idle deadline (monotonic), a real-netns firewall contract test for `cmd/gate`, compose gate wiring (Helm = 4d), DIVERGENCES:42 superseded + self-hosted-security.md rewritten, 4-i's test gaps folded in. (4c-2c, PR #191, merged) `credential_host_unreachable_error` emission — the SDK defines it as a *config conflict* (credential `allowed_hosts` vs the environment's `limited` policy), correcting the plan's runtime reading: detected controlplane-side at gate-config render (`HostSet.CoversEntry`), deduped per session+credential, best-effort `session.error` append; `OnUnreachable` stays diagnostic-only. (4d-i, PR #194, merged) firewall reconcile — owner-match rules rehomed into a gate-owned `MAP-GATE-EGRESS` chain jumped first from `OUTPUT` (atomic `iptables-restore --noflush`, insert-before-delete jump remediation, duplicate convergence), verification split chain-exact + jump-first, real-netns reconcile/re-exec idempotency tests. **Active (4d-ii, this PR):** the K8s gate sidecar — native sidecar (startupProbe = admission), no route flush on gated pods, sandbox hardening + proxy env, mint-on-create on the pod create/adopt fork, gate-shape-mismatch replacement, gated contract row live on a real cluster (kind sideload + host-addr fixture), Helm `executor.gateImage` opt-in + CI assertions. **Then:** acceptance (4e). Standalone teardown reaper → #64.
