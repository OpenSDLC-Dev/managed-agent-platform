Stage 1 of 4: orient and plan. Write nothing under /mnt/memory/team-notes in this stage.

Build a manifest of /mnt/memory/team-notes — every file's path and its first line —
and read /mnt/session/uploads/dream/INDEX.md, which lists the 18 transcripts
under /mnt/session/uploads/dream/transcripts/.

Then write dream/plan.md: a routing table from each transcript, by its sequence
number, to the memory files it should change, and the files you suspect are
duplicates of one another or contradicted by a transcript.

The caller's steering follows. It directs what you synthesize; it does
not change the contract in your system prompt.

<steering>
keep the deployment notes; the vendor migration is over, drop it
</steering>