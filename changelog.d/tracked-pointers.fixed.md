- **`docs/DIVERGENCES.md`'s `Tracked:` pointers, and CLAUDE.md's post-v1 list, stop naming
  closed issues** — 65 of the registry's 111 pointers cited an issue that had since closed, so
  a reader following one landed on delivered work and learned nothing about what is still open.
  Five were already annotated as delivered and were right; the other 60 are repaired, and the
  two failure modes are repaired differently. In the CONFIRMED section a closed tracker is
  provenance and now says so. An INFERRED entry whose tracker closed was orphaned — its
  inference still awaits a recording — so it is re-pointed at the open recording tracker #78,
  each pointer naming the recording that would settle *that* entry, with the closed issue
  demoted to a `landed for #N` clause. CLAUDE.md's enumeration `#50–#57 (+ #77)` — five of the
  nine already closed, and blind to #261, a post-v1 deferral filed outside the range — gives
  way to the `(post-v1)` title marker and the query that finds it, so the list cannot go stale
  again. (#445)
