- **The multiagent advisor entry is a settled refusal, and the registry says so** — a `{type:"advisor", model}` entry in an agent's `multiagent` roster has been refused with `400 entry type must be "agent" or "self"` since the roster shipped; that refusal is now the recorded decision rather than a placeholder for unbuilt work, because an advisor is a model the reference's own service consults mid-turn and a self-hosted coordinator has no analogue to run. Two [docs/DIVERGENCES.md](./docs/DIVERGENCES.md) statements are corrected with it: the roster stopped being "inert at runtime" when slices 3 and 4 put its members on real session threads, and it is the advisor *variant* of the reference's three agent unions that never renders — the unions themselves render on every roster, session and thread response. The plan-35 entries' `Tracked:` pointers move from the closed #53 to #78, which now carries the plan's thirteen-item recording checklist so those inferences keep an open home. (#431)
