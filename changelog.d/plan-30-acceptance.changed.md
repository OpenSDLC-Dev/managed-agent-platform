- **Plan 30 archived — console-issued environment keys accepted against the real `ant` CLI** ([#43](https://github.com/OpenSDLC-Dev/managed-agent-platform/issues/43), [docs/plan/30_environment-keys-console-issuance.md](docs/plan/30_environment-keys-console-issuance.md)) — the issue asked that "an operator can issue and rotate an environment key, and a real `ant beta:worker` authenticates with it end to end (no manual DB edits)", and that is what ran: a key issued by curl against the console API drove a real `ant beta:worker poll` built from the read-only CLI checkout, confirmed positively rather than by silence — the platform's own `…/work/stats` reported a non-zero `workers_polling`, which only an authenticated poll can produce, while answering 401 to the management key, so the worker lane is demonstrably the lane in play. Revoking through the console returned a bodiless 204 and the running `ant` process printed the platform's `authentication_error` envelope and exited. The property the retired rotate-on-mint model could never offer was proven directly, beyond what the issue asked: two named keys, two real workers, revoke one — that host's process exited on the auth error while the other kept polling with a still-valid key and a still-empty log. No manual database access at any point; the only process touching Postgres was the control plane. The full transcript, including why the run used its own throwaway control plane rather than the shared compose stack, is in [docs/HISTORY.md](docs/HISTORY.md). **The plan archived with one of its own criteria unmet**: slice 3's stronger wording asked to poll *until an item is pulled*, which the 2026-08-10 run did not do, and it was carried to [#363](https://github.com/OpenSDLC-Dev/managed-agent-platform/issues/363). The obstacle was causal — three sites enqueue `tool_exec`, but none can produce one without a pre-existing `agent.tool_use`, which no client may post — so it took a model turn. **A second run on 2026-08-11 did it and closed #363:** a `self_hosted` session's first turn emitted `agent.tool_use` for `bash`, and a real `ant beta:worker poll` on a console-issued key claimed the queued item, ran the tool in-process, and settled the result back, which the model then read. Queue delivery, work serialisation and the CLI's tool-runner half — the three properties the first run left unproven — therefore hold too. (The 2026-08-10 record blamed the delay on the model route STATE.md tracks as a blocker; that attribution was wrong, and HISTORY.md says so: that task names the GCP staging secret, while the local route had been configured all along.) Both runs used throwaway infrastructure rather than the `deploy/compose` stack slice 3 names, which is the one substitution in the plan's stated procedure.
