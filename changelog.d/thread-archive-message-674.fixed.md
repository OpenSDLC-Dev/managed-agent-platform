- **Archiving a primary thread is refused in the reference's words** ([#674](https://github.com/OpenSDLC-Dev/managed-agent-platform/issues/674), part 4) — `POST /v1/sessions/{id}/threads/{tid}/archive` on a session's primary thread is still a 400 `invalid_request_error`; its message is now the one the reference returns, "The primary thread cannot be archived; archive the session instead." A client matching the old text ("the primary thread cannot be archived; archive the session") needs the new one.
