- **A spawned child's own event list opens with its status, then its task** ([#675](https://github.com/OpenSDLC-Dev/managed-agent-platform/issues/675), part 3) — A child a coordinator's `create_agent` spawns now lists `session.thread_status_running` before the `agent.thread_message_received` carrying its task, the order every recorded reference child shows; it used to list them the other way round. The session and primary thread views are unchanged: each spawn still reads `session.thread_created` and then `agent.thread_message_sent`, with the child's status cross-posted after them. All four events still commit in one transaction; docs/DIVERGENCES.md records what that leaves different from the reference.
