- **The registry's MCP, egress, skills and environment-key inferences, settled against a second recording** — The second 2026-09-03 wave, 281 request/response pairs against the live managed-agents endpoint, answered fifteen of [docs/DIVERGENCES.md](docs/DIVERGENCES.md)'s open questions and dissolved one. Nine found this platform wrong rather than merely unobserved; eight became issues #570-#574 and #577-#579, covering the private/reserved address floor `unrestricted` keeps and we do not, the create-time `allow_mcp_servers` gate, 403 not being an MCP authentication failure, the composed `mcp__{server}__{tool}` name we cap at 64 bytes where the reference does not, terminated sessions hidden from its default listing, a failed model request terminating rather than settling idle, the session-scoped `file_id` a resource echoes, and where `credential_host_unreachable_error` is emitted from. The ninth is `list_cost.amount`, cents and string-encoded, recorded on #432 before anything reads it as dollars. Two entries were corrected rather than confirmed: `mcp_oauth_validate` never probes anything on this account, whatever the first wave concluded from it, and the file-mount sentinel question dissolves because the reference mounts session storage over FUSE rather than streaming bytes into a sandbox.
