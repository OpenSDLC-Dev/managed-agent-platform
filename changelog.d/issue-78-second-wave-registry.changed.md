- **The registry's MCP, egress, skills and environment-key inferences, settled against a second recording** — The second 2026-09-03 wave, 281 request/response pairs against the live managed-agents endpoint, settled fifteen of the archive's open comparison entries, narrowed two and dissolved one, touching twenty [docs/DIVERGENCES.md](docs/DIVERGENCES.md) entries. Eleven findings said this platform was wrong rather than merely unobserved, and ten became issues #570-#574, #577-#579 and #581-#582: the private/reserved address floor `unrestricted` keeps and we do not, the create-time `allow_mcp_servers` gate, 403 not being an MCP authentication failure, the composed `mcp__{server}__{tool}` name we cap at 64 bytes where the reference does not, terminated sessions hidden from its default listing, a failed model request terminating rather than settling idle, the session-scoped `file_id` a resource echoes, where `credential_host_unreachable_error` fires from, the Level-1 skills block's XML-tagged template, and a clone that is shallow with two remotes there and full with one here. The eleventh, `list_cost.amount` in cents, went to #432, and #576 is now registered where the registry had been silent on it. Two entries were corrected rather than confirmed: `mcp_oauth_validate` probes nothing on this account, and the file-mount sentinel question dissolves because the reference mounts session storage over FUSE rather than streaming it.
