- **The registry's MCP, egress, skills and environment-key inferences, settled against a second recording** — The second 2026-09-03 wave, 281 request/response pairs against the live managed-agents endpoint, answered fifteen of [docs/DIVERGENCES.md](docs/DIVERGENCES.md)'s open questions and dissolved one. Six found this platform wrong rather than merely unobserved; five became issues. `unrestricted` egress keeps a private/reserved address floor we do not (#570); `allow_mcp_servers` is enforced at session create as a 400 naming the host, not at dial time (#571); only 401 is an MCP authentication failure where we also count 403 (#572); the reference composes `mcp__{server}__{tool}` for the model too, reserves that prefix, and carries no 64-byte ceiling, so our cap silently declines to offer tools it offers (#573); and a terminated session is hidden from its default session listing but not from ours (#574). The sixth is `list_cost.amount`, which is cents, string-encoded — recorded on #432 before anything reads it as dollars. Two entries were corrected rather than confirmed: `mcp_oauth_validate` never probes anything on this account, whatever the first wave concluded from it, and the file-mount sentinel question dissolves because the reference mounts session storage over FUSE rather than streaming bytes into a sandbox.
