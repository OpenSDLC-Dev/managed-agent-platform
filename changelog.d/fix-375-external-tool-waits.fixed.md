- **External tool waiting and recovery (#375)** — Custom and self-hosted tool calls now advertise `requires_action` and resume through the ordinary events API. Replies and approvals received out of order are retained and processed in each thread's call order; cloud tools can finish between custom replies, and self-hosted approval continues waiting for the worker result. Only fully settled threads resume the model. Interrupt/archive preserve received replies and cancel the rest. Upgrade workers to accept running/idle sessions before switching controlplane, brain and executor together; legacy running waits remain recoverable.
