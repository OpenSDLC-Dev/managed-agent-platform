- **Two registry entries now name the issue that decides them** (#680) — the [docs/DIVERGENCES.md](docs/DIVERGENCES.md) entries for the work object's `id`, which the reference fills with the session's id where we use `work_…`, and for the reference's `actor: null` key, which the published schema lacks and we omit, were tracked only by the standing recording tracker, #78. [#680](https://github.com/OpenSDLC-Dev/managed-agent-platform/issues/680) is the open issue that asks whether to follow the recording or the published schema on those two points, so each entry now names it first. No divergence text changed.
