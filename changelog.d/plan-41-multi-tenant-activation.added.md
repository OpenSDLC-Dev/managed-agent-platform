- **Plan 41 drafted, and its gating recording taken — multi-tenant activation** ([docs/plan/41_multi-tenant-activation.md](./docs/plan/41_multi-tenant-activation.md), [#56](https://github.com/OpenSDLC-Dev/managed-agent-platform/issues/56)) — the design for the remaining half of #56: the reserved `org_id`/`workspace_id`/`project_id` columns become real scoping, with the **workspace as the isolation unit** and org and project frozen at `default`. Every credential resolves to exactly one workspace at authentication; every statement on a scoped table carries the predicate, is admitted by a named guard rule, or is a reviewed exemption; a resource in another workspace answers exactly as an absent one. Enforcement is application-level predicates behind a source-reading completeness guard, plus a composite workspace foreign key; row-level security is rejected because the Postgres suite runs as superuser. Six slices, the operator surface last. The plan corrects the issue's inherited sizing — 211 statements across 32 files, not 60–80 across 13. **The two-workspace recording slice 1 was gated on was taken 2026-09-05**, answering all six items and closing every NOT OBSERVED entry: a cross-tenant id answers 404 indistinguishably from an absent one, an environment key carries no workspace at all, `anthropic-organization-id` is a bare UUID, and an archived workspace's key is refused as an unknown one. The plan is **41**, not 40, which #353's environment-packages plan took first.
