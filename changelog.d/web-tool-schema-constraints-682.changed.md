- **The web tools' input schemas carry the reference's constraints** ([#682](https://github.com/OpenSDLC-Dev/managed-agent-platform/issues/682), part 1) — The schemas the model is handed for `web_fetch` and `web_search` now match what a 2026-09-02 recording of the reference echoed: `url` carries `"format": "uri"`, `query` carries `"minLength": 2`, and both objects carry `"additionalProperties": false`. Both provider adapters pass the keywords to the model untouched; the OpenAI-compatible one sets no `strict`, whose schema subset excludes the `uri` format and `minLength`. The executor's `web_search` check now holds the same floor: a query under two characters, counted after trimming, answers an `is_error` result instead of reaching the search backend. The tools' descriptions are unchanged and remain a recorded divergence; whether to adopt the reference's, live date line included, stays open on #682.
