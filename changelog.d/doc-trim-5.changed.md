- **`docs/DIVERGENCES.md` becomes the registry its own header promises** (#413) — Twenty-four
  entries had grown into design essays restating derivations their code comments already carry;
  each is now a row that states the surface, what this platform does, whether that is CONFIRMED
  or INFERRED, and where the reasoning lives. 313 KB → 227 KB with every entry, section header
  and citation intact. Each row was drafted and then attacked by an independent reviewer whose
  only job was to find claims surviving neither in the row nor anywhere in the repo: twenty-five
  did, and were restored — among them a published `pause_turn` sentence that is the
  turn-classification entry's own strongest counter-evidence, the reproducible
  `docker --storage-opt` measurement, and the strip-not-reject argument the 0.2.0 changelog
  points back here for. Six accuracy defects were fixed rather than carried forward, four of
  them inherited from the original entries: an error message named as the only one lacking a
  field path where the code has three, a field the SDK's error union requires on every variant
  presented as this platform's invention, a bounded path-id enumeration widened to "every", and
  a two-leg reachability argument reduced to one leg and asserted over both. The three
  "v1.62.0 checkout" citations are settled against the pinned v1.61.0 they were always read at.
